const sayHi = (name) => "Hello " + name;

const largeNumber = (num1, num2) => num1 > num2 ? num1 : num2;

const largeNumberBoolean = (num1, num2) => num1 > num2;

module.exports = { sayHi, largeNumber, largeNumberBoolean };